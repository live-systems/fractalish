language: python
python:
  - "3.5"
#  - "2.7"
# command to install dependencies
before_install: ./configure
install: make
# command to run tests
script: 
  make test

addons:
  apt:
    packages:
    - nginx